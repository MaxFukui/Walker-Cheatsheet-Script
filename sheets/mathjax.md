# MathJax - Math Formulas

~ Inline: $...$  — Block: $$...$$
~ Commands are case-sensitive

hat on variable | \hat{A}
if and only if | \iff
equivalent | \equiv
triangle | \triangle
similarity | \sim
line break | \\
angle | \angle
congruent (same shape/size) | \cong
case with multiple rows | \begin{cases} \end{cases}
